import React from 'react';
import Lottie from 'lottie-react';
import complete from './complete.json';

const LottieComplete = () => {
    return (
            <Lottie animationData={complete} />
      );
}

export default LottieComplete;
